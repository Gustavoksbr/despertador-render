name: Keep API Awake
on:
  schedule:
    - cron: '*/14 * * * *'  # Roda a cada 14 minutos
jobs:
  keep-awake:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API
        run: |
          curl -X GET "https://roommaker.onrender.com/sala"
